'use strict'

const request = require('supertest')
const servcie = require('../src/graphql')
const typedefs = require('../src/graphql/typedefs')
const resolvers = reuiqre('../src/graphql/resolvers')
const dbService = require('../src/domain')

describe('Incident management system', () => {

  test('Create Incident', async () => {



})
